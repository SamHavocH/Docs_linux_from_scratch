# Day 1 

## Goals of today:

- decide the purpose of the OS 
- follow at least one hour of tony's video

## Catalog:

* we installed the wrong version of the gentoo image at our virtualbox, we were using a minimal instalation instead of the liveGUI media
* after getting it right we created the swap partition, the efi partition and the root partition
* we've set the $LFS variable for better access to our mount point
* the sources folder was created and populated using wget

### Note: 

* we did some parts multiple time due to bad virtual machine configuration, so we could not get to the first hour of the video
